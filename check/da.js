/*async function main() {
  const ar = [1,2,3];
  const ar1 = [...ar, 4];
  await Promise.all(ar1.map(async e => {
    return e + 1;
  }));
}
main().then(() => console.log('kek'));
*/
console.log('kek');
